import { LoginForm } from "@/components/auth/LoginForm";

export default function LoginPage() {
  return (
    <div className="hero-gradient flex min-h-[calc(100vh-10rem)] items-center justify-center px-4">
      <LoginForm />
    </div>
  );
}
